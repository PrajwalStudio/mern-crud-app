#Step 1: Create Server folder
#Step 2: Create index.js 
#Step 3: In terminal,npm init -y
#Step 4: Install express,cors,mangoose,nodemon
npm i mongoose -(a library to connect mongoDB database)
npm i express -(a web framework responsible for handling requests and managing response)
npm i cors    -cross origin resource sharing(it is a security feature in web browser)
npm i nodemon -(used to run and automatically restarts the server when you change code.)